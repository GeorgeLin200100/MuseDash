# MuseDash
FDU FPGA Class Project "Muse Dash On FPGA"

## 前端启动方式
1. 终端切换到项目根目录 `MuseDash`：
   ```bash
   cd E:\Study\FPGA2\MuseDash
   ```
2. 启动本地静态服务（Python 内置）：
   ```bash
   python -m http.server 8000
   ```
3. 浏览器访问前端入口：
   ```
   http://localhost:8000/frontend/
   ```
