# Music Sync（软硬件协同）

MVP：启动程序后按空格播放对应音乐；若当前谱面无同名音乐文件，则播放节拍提示而非静音。

建议组件：
- `player.py`：控制播放，判断是否存在同名音频，回退到节拍合成。
- `assets/`：默认音乐/节拍文件存放处（可按需扩展为外部路径）。

TODO：
- 选择音频后端（如 pygame.mixer / pyaudio）。
- 定义谱面文件名与音频文件名的匹配策略（同名不同后缀）。
- 提供最小 CLI：加载谱面，等待空格触发播放/暂停。
